# timo.strotmann.me

Personal website and blog built with Astro and Tailwind CSS. Content lives in `src/content/blog` (Markdown/MDX), pages in `src/pages`, and shared UI in `src/components`.

## Development

- Install: `npm install`
- Run: `npm run dev`
- Build: `npm run build`
- Preview: `npm run preview`
